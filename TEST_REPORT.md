# Test report

CI performs a clean Gradle build and checks that the APK exists and is non-empty. Full agent acceptance tests remain blocked until the remote backend and model credential are configured. No claim is made that those tests have passed.
